project('01_intro', 'c',
  default_options: [
    'buildtype=debugoptimized',
    'c_std=c11',
    'warning_level=2',
  ],
)

executable('01_intro', 'src/main.c',
  dependencies: dependency('libdrm')
)
